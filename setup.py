from setuptools import setup, find_packages

setup(
    name='finances_back_end',
    version='1.0',
    description='Pacote contendo projeto de back end sobre finanças',
    install_requires=['datetime', 're'],
    packages=find_packages()
)